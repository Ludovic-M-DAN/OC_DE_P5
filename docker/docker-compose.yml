version: '3.8'

services:
  mongo:
    image: mongo:5.0
    container_name: healthcare_mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: secure_password
    volumes:
      - mongodb_data:/data/db
    networks:
      - healthcare_net
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.runCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  setup_auth:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: healthcare_setup_auth
    depends_on:
      mongo:
        condition: service_healthy
    environment:
      - MONGO_HOST=mongo
      - MONGO_USER=admin
      - MONGO_PASSWORD=secure_password
    volumes:
      - ../src:/app/src:ro
    networks:
      - healthcare_net
    command: ["python", "src/setup_auth.py"]

  migration:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: healthcare_migration
    depends_on:
      setup_auth:
        condition: service_completed_successfully
    environment:
      - MONGO_HOST=mongo
      - MONGO_USER=migration_user
      - MONGO_PASSWORD=migration_secure_2024
      - MONGO_AUTH_DB=healthcare_db
    volumes:
      - ../data:/data:ro
      - ../src:/app/src:ro
    networks:
      - healthcare_net

volumes:
  mongodb_data:

networks:
  healthcare_net:
    driver: bridge
